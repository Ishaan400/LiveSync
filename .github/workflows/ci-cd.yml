name: CI/CD

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        env:
          NODE_ENV: test
          JWT_SECRET: testsecret
        run: npm test

  build-and-push:
    name: Build and push images to GHCR
    runs-on: ubuntu-latest
    needs: test
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Derive lower-case owner for GHCR
        shell: bash
        run: echo "GHCR_OWNER=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push API image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ghcr.io/ishaan400/livesync-backend-api:latest
            ghcr.io/ishaan400/livesync-backend-api:sha-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push Automerge WS image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.automerge
          push: true
          tags: |
            ghcr.io/ishaan400/livesync-backend-automerge:latest
            ghcr.io/ishaan400/livesync-backend-automerge:sha-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # deploy-railway job intentionally removed per user request


